// Creating the main application in an immediately invoked function expression (IIFE)

// (function(){ 
  angular
    .module("MainApp", [
      'templates',
      'ngResource',
      'ngRoute'
      ]);

// })();  